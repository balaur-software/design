# Implementation Plans

Generated by the improve skill (deep audit) on 2026-07-08, against commit
`74aad33`. Execute in the order below unless dependencies say otherwise. Each
executor: read the plan fully before starting, run its drift check, honor its
STOP conditions, and update your row when done.

Repo-wide rule binding every plan: **never commit or push unless the owner
explicitly asks** — plans leave verified changes in the working tree.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Wire the Storybook interaction suite into a runnable gate (`check:full`) | P1 | M | — | DONE (approved; branch `worktree-agent-a5c0b697980639e9c`, commit `26bcec1` — awaiting owner merge) |
| 002 | Commit agent instructions — track `.claude/skills/`, add AGENTS.md | P1 | S | — (soft: 001) | DONE (approved; branch `worktree-agent-a31ea8a3b5f92ac50`, commit `6eddcda` — awaiting owner merge) |
| 003 | Hygiene bundle: RadioGroup tab stop, delete gen-barrel.mjs, token-fallback sweep, BlockRenderer exhaustiveness | P2 | M | — (soft: 001) | DONE (approved; branch `worktree-agent-a954e05d4aedc986d`, commit `bcb327a`, includes 001 merged — awaiting owner merge) |
| 004 | Docs corrections: README lint section, CONSUMING pins, license decision | P2 | S | — (soft: 003, see Step 4) | DONE (approved; branch `worktree-agent-a949856541e3a6a80`, commit `4914f31`, includes 001 merged — awaiting owner merge. OPEN owner items: license choice; README:5 provenance link `harness` vs `balaur-life`) |
| 005 | Toolchain migrations: Biome config `preset`, Vite 7 | P2 | S | — (soft: 001) | DONE (approved; branch `worktree-agent-a50e0be3a1d31b90d`, commit `3bfa153`, includes 001 merged — awaiting owner merge) |
| 006 | Characterize foundation hooks: useControllableState, useFocusTrap | P2 | M | 001 | DONE (approved; branch `worktree-agent-a56ee86db55ebfff8`, commit `7f5de95`, includes 001 merged — awaiting owner merge) |
| 007 | Pin documented interaction contracts: overlay focus/scroll, Select keyboard, Calendar boundaries | P2 | M | 001 | DONE (approved; branch `worktree-agent-a4b289fe718a58e12`, commit `ace929e`, includes 001+003 merged — awaiting owner merge) |
| 008 | Preserve force-layout positions across node-set changes | P2 | M | — | DONE (approved; branch `worktree-agent-a501ad085f09b5495`, commit `5d2e4cc` — awaiting owner merge) |
| 009 | Cut MemoryGraph per-frame render cost (memo atoms, drag-path fix) | P2 | M | 008 | DONE (approved; branch `worktree-agent-a4609cedfd76f6d55`, commit `7dd7b9b`, includes 008 merged — awaiting owner merge; note: plan's "≤1 find" grep criterion was advisor-miscalibrated, intent verified instead) |
| 010 | Reuse ImageData in octant-core painters | P2 | S | — | DONE (approved; branch `worktree-agent-a3faf0781ab249ffd`, commit `28fc2b1` — awaiting owner merge) |
| 011 | SPIKE: visual regression snapshots on existing browser-test infra | P3 | M | 001 | DONE (approved, GO-WITH-CAVEATS; branch `worktree-agent-a9e94500209677752`, commit `b0ca112`, includes 001 merged — findings at plans/011-findings.md on that branch) |
| 012 | DESIGN: composer input surface (attachments + slash commands) | P3 | M | — | DONE (approved after 1 revision; branch `worktree-agent-ad9157db411375430`, commit `0b50478` — awaiting owner merge) |
| 013 | DESIGN: `<OctantRoot>` app-root provider | P3 | S–M | — | DONE (approved after 1 revision; branch `worktree-agent-ade55cc8a51d88963`, commit `90f391b` — awaiting owner merge) |
| 014 | BUILD: OctantRoot component + CONSUMING rewrite + FONT_MONO_SUBPATH | P2 | S–M | merged main (013's spec) | DONE (merged to main, `ef97fe4`) |
| 015 | BUILD: composer attachments + slash commands + AttachmentChip | P2 | L | merged main (012's spec) | DONE (merged to main, `3f376b9`; shared a branch with 014 after a worktree-provisioning mixup — both commits reviewed separately, combined branch gate-verified) |
| 016 | BUILD: VRT rollout — baselines for every story's primary export | P2 | M | 014, 015 | DONE (merged to main, `0f1d1de`; 115 baselined, 8 reasoned skips; discovered MemoryGraph's tick loop never stops under reduced motion — follow-up candidate) |
| 017 | RELEASE PREP: AGPL-3.0 LICENSE, provenance fix, v0.4.0 lockstep | P1 | S | 014, 015, 016 | DONE (merged to main, `68355e0`; tagged `v0.4.0`) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Execution record (2026-07-08)

All 13 plans executed by dispatched Sonnet executors in isolated worktrees,
reviewed/approved by the advisor, and — with the owner's explicit go-ahead —
**MERGED into `main`** (27 commits, `74aad33..586b319`) in the order
001 → 002 → 010 → 008 → 003 → 009 → 005 → 004 → 006 → 007 → 011 → 012 → 013.
Executor worktrees and branches deleted after merge (`git branch -d` verified
each was fully merged). NOT pushed — pushing is the owner's step.

Merge notes:
- The only conflict was plan 002's `.claude/skills/*` (add/add): the owner had
  independently committed newer skill versions (`b79f765`) between planning and
  merge; resolution kept the owner's versions, took AGENTS.md/CLAUDE.md from
  the plan branch. Recorded in the merge commit message.
- Post-merge verification on `main`: `bun run check:full` exit 0 (lint clean,
  261 unit tests, 123/123 story files, 484/484 browser tests), VRT spike 3/3,
  token-divergence scanner silent.
- Operational gotcha discovered: executor worktrees living under
  `.claude/worktrees/` break `biome check .` from the repo root (nested
  biome.json discovery). Remove worktrees before linting, or park future
  worktrees outside the repo directory.

## Second wave (2026-07-08, owner-directed "do all of them")

Plans 014–017 written and executed same-day: OctantRoot build (`ef97fe4`),
composer attachments + slash commands (`3f376b9`), VRT rollout (`0f1d1de`,
115 baselines / 8 reasoned skips via `bun run test-vrt`), release prep
(`68355e0` = `chore(release): v0.4.0`, tagged `v0.4.0`). All release gates
green on the tagged commit: check:full (271 unit + 490 browser tests),
test-vrt, build-storybook. License ratified: **AGPL-3.0-or-later** (owner
decision, mirrors `memory`). Provenance fixed: extraction source is
`balaur-software/web` (balaur-life), evidence-verified.

## Third wave (2026-07-08, "fix the two open threads")

- **Thread 1 — web pin bump: DONE.** `web/package.json` pinned
  `@balaur/octant` `#v0.3.0` → `#v0.4.0`, `bun install` (resolved design@68355e0
  = the v0.4.0 tag), `bun run check` in web GREEN (typecheck 0, lint clean, 14
  tests). No web source change needed — a pre-fix parallel diagnosis + adversarial
  review confirmed web never uses the removed `attachHint`/`slashHint` props.
  web's package.json + bun.lock left UNCOMMITTED for the owner. (Aside: web's
  biome.json has the same `recommended`-deprecation design's plan 005 fixed —
  pre-existing, not touched.)
- **Thread 2 — MemoryGraph reduced-motion loop: plan 018, IN PROGRESS.** A
  Workflow diagnosis CONFIRMED the bug (under reduced motion `converged` never
  flips, so MemoryGraph re-renders at 60fps forever) and its adversarial-verify
  stage REJECTED the naive one-file fix — it silently regresses reduced-motion
  redraws (posById is memoized on MemoryGraph's own tickFrame ref, which the
  hook's forceTick never bumps). Plan 018 carries the corrected two-file fix
  (useForceLayout `converged: reduced||converged` + wake() forceTick; MemoryGraph
  posById inline, tickFrame removed) plus a probe that discriminates buggy /
  half-fixed / fixed, and un-skips memorygraph-default in VRT.

| Plan | Title | Status |
|---|---|---|
| 018 | Fix MemoryGraph perpetual re-render loop under prefers-reduced-motion | DONE (merged to main, `ecae783`; check:full + test-vrt ×2 green; memorygraph-default un-skipped; new reduced-motion.vrt.tsx probe discriminates buggy/half-fixed/fixed) |

**Release state: v0.4.1 CUT.** `main` = `c1fe89b` (`chore(release): v0.4.1`),
tagged `v0.4.1` locally. Contains the MemoryGraph fix (`ecae783`) + lockstep
0.4.0→0.4.1 bump. All release gates green on the tag (check:full, build-storybook,
test-vrt ×2). **NOT pushed** — owner runs `git push origin main && git push origin v0.4.1`.

`web/package.json` re-pinned `#v0.4.0` → `#v0.4.1` (uncommitted). Its `bun.lock`
still resolves the v0.4.0 commit — after pushing the design v0.4.1 tag, run
`cd web && bun install` (fetches design@v0.4.1, updates the lock) then commit web.
web doesn't render MemoryGraph and v0.4.0→v0.4.1 has zero consumer-API change, so
the re-pin is track-latest hygiene; web's typecheck outcome is unchanged.

Known VRT-harness note (018): `memorygraph-default` is deterministic under the
real `bun run test-vrt` gate (passes twice) but a single isolated `-t` run can
show a ~1% warm-up diff (byte-identical actuals across isolated reruns) — a
phase-2 VRT robustness item, not a regression. Also pre-existing from v0.4.0:
`packages/ui/src/__vrt__` (test scaffolding + 1.2MB baselines) is NOT excluded
from the published `files` glob — a publish-size follow-up, harmless to consumers
(nothing in `__vrt__` is imported by the barrel; web typechecked green with it present).

**Remaining owner items (unchanged from v0.4.0):**
- Commit web's pin bump (`web/package.json` + `bun.lock`) when ready.
- Resolved-but-notable: three bright border fallbacks (ResizableSplit.tsx:55,
   BootOverlay.stories.tsx:48, ResizableSplit.stories.tsx:81) were aligned to
   canonical dim values — if a bright border was INTENDED, change the var, not
   the fallback.

**Follow-up candidates discovered during builds** (not planned yet):
- MemoryGraph's re-render tick loop never stops under `prefers-reduced-motion`
  (its `converged` gate can't flip when the sim never runs) — found by VRT.
- VRT determinism fixes for the 8 skipped stories (unseeded Math.random,
  unconditional rAF loops, IntersectionObserver race) — reasons inline in
  `packages/ui/src/__vrt__/vrt-stories.vrt.tsx`.
- ChatPanel-level attachments/commands prop threading (deferred from 015).
- VRT phase 2: CI-generated baselines, accent/viewport matrix, non-primary exports.

## Dependency notes

- **001 first.** It makes 464 existing browser-mode tests runnable and green
  (`check:full`); plans 006, 007, and 011 add tests that live in that gate,
  and 003/005/008/009 use it as their verification net where available.
- **008 before 009** — both touch the MemoryGraph/force-layout subsystem;
  009's drift check assumes 008's change has landed.
- **004 Step 4 reads 003's status** — a CONSUMING.md token-hygiene bullet only
  becomes stale once 003's fallback sweep lands.
- 002, 010, 012, 013 are fully independent and parallelizable.
- Soft dependencies ("soft:") mean the plan is valid standalone but its
  verification is stronger after the referenced plan lands.

## Findings considered and rejected (do not re-audit)

From the 2026-07-08 deep audit (full findings table in the audit conversation;
categories: correctness, security, perf, tests, tech debt, deps, DX, docs,
direction):

- **"Empty design-octant-reference skill" (DX)** — mis-attributed by an audit
  subagent; `SKILL.md` + `references/components.md` exist (they were merely
  untracked — plan 002 covers that).
- **Stray `balaur-octant-0.1.0.tgz` in repo root** — gitignored local
  artifact; `git clean` territory, not a plan.
- **bun.lock stale root workspace name (`balaur-design`)** — metadata only;
  self-heals on the next `bun install` (plan 005 regenerates the lock anyway).
- **Duplicate `@vitest/expect@3.x` in the tree** — pinned internally by
  `storybook@10.4.6` itself; upstream-controlled, not actionable.
- **"No CI" as a standalone finding** — CI is the delivery vehicle for
  `check:full` (plan 001) plus a runner choice the owner hasn't made; noted in
  001's maintenance notes instead.
- **Security posture** — audited clean: no XSS sinks, no secrets, no
  prompt-injection content, `bun audit` clean. One LOW-confidence hardening
  idea (scheme-validating `href` props in InlineCitation/NavMenu/Breadcrumb)
  was not selected for planning; recorded here so a future pass starts there.
- **SSR discipline** — CONSUMING.md's claims verified accurate; no findings.
- **Not selected this round** (real findings, deliberately deferred by the
  owner): atoms importing upward into organism type modules (~14 files);
  `useRovingFocus` extraction for the ~9 duplicated keyboard-nav
  implementations (do AFTER 006/007 exist as characterization net);
  `ChatMessage`/`ChatThread` memoization; a11y axe gate flip
  (`test: "todo"` → `"error"`, backlog triage first); TypeScript 6 bump;
  Vite 8 (+ `@vitejs/plugin-react` ^6); coverage script/threshold;
  kebab-case test-file renames; `Stepper.fillColor` deprecated-alias removal;
  `ComponentCatalog` hardcoded-inventory drift; chart axis/tooltip/legend
  primitives (direction); memory-graph write surface (direction — needs an
  owner decision on direct-edit vs consent-flow first).

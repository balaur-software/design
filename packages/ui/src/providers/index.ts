/**
 * Context providers — the infrastructure components that wrap the app and
 * expose shared context (accent theme, toasts, etc.) to descendants.
 */

export * from "./AccentProvider/AccentProvider";
